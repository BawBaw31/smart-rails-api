{% extends 'base.html.twig' %}

{% block title %}VisitType{% endblock %}

{% block body %}
    <h1>VisitType</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ visit_type.id }}</td>
            </tr>
            <tr>
                <th>Label</th>
                <td>{{ visit_type.label }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_visit_type_index') }}">back to list</a>

    <a href="{{ path('app_visit_type_edit', {'id': visit_type.id}) }}">edit</a>

    {{ include('visit_type/_delete_form.html.twig') }}
{% endblock %}
